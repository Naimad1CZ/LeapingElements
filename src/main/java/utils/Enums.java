package utils;

public class Enums {
    public enum HeroType {
        fire,
        ice
    }

    public enum TurretAndProjectileType {
        fire,
        ice,
        combined
    }

    public enum TileType {
        out,
        air,
        solid,
        water
    }
}
